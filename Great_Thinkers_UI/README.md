# Great Thinkers UI

See the [project README](../README.md) for setup, the Windows launcher, privacy details, and verification commands.

Run `npm run dev` for development, or `npm run build` followed by `npm start` for normal use. Requires Node.js 24+ and local Ollama.
